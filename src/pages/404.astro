---
import Layout from "@/layouts/Layout.astro";
import GlobeError from "@/components/GlobeError.vue";

const title = "Dima Portfolio | Oops Lost...";
const description = "The page you're looking for seems to have drifted away.";
---

<Layout title={title} description={description}>
  <section class="absolute inset-0 flex items-center justify-center">
    <GlobeError client:only="vue" />
  </section>
</Layout>
